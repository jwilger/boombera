#!/usr/bin/env ruby
$:.unshift(File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib')))
require 'boombera'

database, path, body = ARGV[1..3]
db = CouchRest.database!("http://127.0.0.1:5984/#{database}")
db.save_doc({:path => path, :body => body})
puts "Content Created: #{path}"
